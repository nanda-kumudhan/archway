set $term foot
set $file_manager Thunar
set $file_menu exec rofi -show filebrowser -show-icons -icon-theme "Papirus-Dark"
set $power_menu exec sh -c 'choice=$(printf "Logout\nCancel" | rofi -dmenu -i -p "Session"); [ "$choice" = "Logout" ] && swaymsg exit'
set $editor zeditor

exec dunst
exec kanshi
exec autotiling
exec --no-startup-id keepassxc --minimized

exec --no-startup-id gnome-keyring-daemon --start --components=pkcs11,secrets,ssh
exec_always dbus-update-activation-environment --systemd WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP XDG_SESSION_DESKTOP PATH
exec systemctl --user import-environment SWAYSOCK WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_DESKTOP

exec systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway

# App launchers
set $launcher exec rofi -show drun -show-icons -icon-theme "Papirus-Dark" -drun-display-format "{name}" -p "Apps"

# --- Workspaces ---
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"
xwayland enable
# --- Theme ---
set $m_bg #121212
set $m_active #8ab4f8
set $m_inactive #2a2a2a
set $m_urgent #f28b82
set $m_text #e8eaed

font pango:JetBrainsMono Nerd Font 10
default_border pixel 1
default_floating_border pixel 1
titlebar_border_thickness 0
titlebar_padding 1 0
title_align center
show_marks no

gaps inner 2
gaps outer 2
smart_gaps off
smart_borders on
hide_edge_borders smart

exec udiskie --smart-tray --automount

client.focused #262626 $m_bg $m_text $m_active #262626
client.focused_inactive #1f1f1f $m_bg #a0a4a8 #1f1f1f #1f1f1f
client.focused_tab_title #262626 $m_bg $m_text
client.unfocused $m_bg $m_bg #6f7378 $m_inactive $m_inactive
client.urgent $m_urgent $m_bg $m_urgent $m_urgent $m_urgent

# --- Startup ---

# --- Session Services ---
exec $HOME/.local/bin/start-polkit-agent

# Export the Wayland session to DBus/systemd.
exec dbus-update-activation-environment --systemd WAYLAND_DISPLAY DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP
exec systemctl --user import-environment WAYLAND_DISPLAY DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP
exec systemctl --user start sway-session.target

# Wallpaper
exec swaybg -i ~/Pictures/wallpaper

# --- Input ---
seat * xcursor_theme Adwaita 24
input * xkb_layout gb

input "type:touchpad" {
    tap disabled
    natural_scroll enabled
    dwt enabled
    middle_emulation enabled
}

input "type:pointer" {
	natural_scroll enabled
}

focus_follows_mouse no

# --- Idle & Power ---
# Lock and power management
exec swayidle -w \
    before-sleep 'swaylock -f'

# --- Keybinds ---

# Launchers

bindsym Ctrl+Mod1+t exec $term
bindsym Mod4+e exec $file_menu
bindsym Mod4+Shift+e exec $file_manager
bindsym Mod4+space exec $launcher
bindsym Mod4+Shift+q kill
bindsym Mod4+l exec swaylock
bindsym Ctrl+Escape exec $term -T "sysmon" -e htop
bindsym Mod4+w exec brave-origin

# Full screenshot
bindsym Print exec grim ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png

# Region screenshot
bindsym Shift+Print exec grim -g "$(slurp)" ~/Pictures/Screenshots/$(date +'%Y-%m-%d_%H-%M-%S').png

# Screen recording (Ctrl + Print to toggle, Ctrl + Shift + Print for area)
bindsym Ctrl+Print exec sh -c 'mkdir -p ~/Videos/Recordings && if pgrep -x wf-recorder >/dev/null; then pkill -INT -x wf-recorder; else wf-recorder -f ~/Videos/Recordings/$(date +'"'"'%Y-%m-%d_%H-%M-%S'"'"').mp4; fi'
bindsym Ctrl+Shift+Print exec sh -c 'mkdir -p ~/Videos/Recordings && if pgrep -x wf-recorder >/dev/null; then pkill -INT -x wf-recorder; else wf-recorder -g "$(slurp)" -f ~/Videos/Recordings/$(date +'"'"'%Y-%m-%d_%H-%M-%S'"'"').mp4; fi'
# Window management
bindsym Mod4+h splith, exec $layout_titles refresh
bindsym Mod4+v splitv, exec $layout_titles refresh
bindsym Mod4+r layout toggle split, exec $layout_titles refresh
bindsym Mod4+f fullscreen toggle
bindsym Mod4+d floating toggle
bindsym Mod4+Shift+d sticky toggle
bindsym Mod4+Shift+p exec wdisplays
bindsym Mod4+c floating enable, resize set width 80 ppt height 80 ppt, move position center
bindsym Mod4+t layout tabbed, exec $layout_titles refresh
bindsym Mod4+Shift+r reload
bindsym Mod4+s layout stacking, exec $layout_titles refresh

bindsym Mod4+plus resize grow width 40 px
bindsym Mod4+minus resize shrink width 40 px

bindsym Mod4+Left focus left
bindsym Mod4+Down focus down
bindsym Mod4+Up focus up
bindsym Mod4+Right focus right
bindsym Mod4+a focus parent
bindsym Mod4+Shift+a focus child

bindsym Mod4+Shift+Left move left
bindsym Mod4+Shift+Down move down
bindsym Mod4+Shift+Up move up
bindsym Mod4+Shift+Right move right

# Workspaces
bindsym Mod4+1 workspace number $ws1
bindsym Mod4+2 workspace number $ws2
bindsym Mod4+3 workspace number $ws3
bindsym Mod4+4 workspace number $ws4
bindsym Mod4+5 workspace number $ws5
bindsym Mod4+6 workspace number $ws6
bindsym Mod4+7 workspace number $ws7
bindsym Mod4+8 workspace number $ws8
bindsym Mod4+9 workspace number $ws9
bindsym Mod4+0 workspace number $ws10

bindsym Mod4+Shift+1 move container to workspace number $ws1
bindsym Mod4+Shift+2 move container to workspace number $ws2
bindsym Mod4+Shift+3 move container to workspace number $ws3
bindsym Mod4+Shift+4 move container to workspace number $ws4
bindsym Mod4+Shift+5 move container to workspace number $ws5
bindsym Mod4+Shift+6 move container to workspace number $ws6
bindsym Mod4+Shift+7 move container to workspace number $ws7
bindsym Mod4+Shift+8 move container to workspace number $ws8
bindsym Mod4+Shift+9 move container to workspace number $ws9
bindsym Mod4+Shift+0 move container to workspace number $ws10

bindsym Mod4+Ctrl+Right move workspace to output right
bindsym Mod4+Ctrl+Left move workspace to output left

bindsym Mod4+Tab workspace next
bindsym Mod4+Shift+Tab workspace prev
bindsym Mod1+Tab focus next window
bindsym Mod1+Shift+Tab focus prev window

# Session actions
bindsym Mod4+Shift+s exec systemctl suspend

# App shortcuts
bindsym Mod4+k exec keepassxc --minimized
bindsym Mod4+z exec $editor

# Scratchpad: hide/show any focused window
bindsym Mod4+Shift+grave move scratchpad
bindsym Mod4+grave scratchpad show


# --- Function Keys ---

# Audio
bindsym XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 1%-
bindsym XF86AudioRaiseVolume exec "wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 1%+"
bindsym XF86AudioMicMute exec wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# System / media
bindsym XF86Tools exec pavucontrol
bindsym XF86Display exec wdisplays
bindsym XF86AudioPrev exec playerctl previous
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86MonBrightnessDown exec "brightnessctl s 3%- -n 0.3l%"
bindsym XF86MonBrightnessUp exec "brightnessctl set +3%"
bindsym XF86AudioStop exec playerctl stop

# Power / lid
bindsym Ctrl+Alt+Delete exec $power_menu
bindswitch --reload --locked lid:on output eDP-1 disable
bindswitch --reload --locked lid:off output eDP-1 enable

# Media (3-finger)
bindgesture swipe:3:left exec playerctl previous
bindgesture swipe:3:right exec playerctl next
bindgesture swipe:3:up exec playerctl play-pause

# Audio mixer
bindsym Mod4+XF86AudioMute exec pavucontrol

# Network manager terminal UI
bindsym Mod4+n exec foot -e nmtui

# Bluetooth terminal UI
bindsym Mod4+b exec foot -e bluetoothctl

# --- Window Rules ---

# Floating dialogs and utilities
for_window [app_id="keepassxc"] floating enable, resize set 74% 80%, move position center
for_window [class="KeePassXC"] floating enable, resize set 74% 80%, move position center
for_window [app_id="thunar"] floating enable, resize set 84% 84%, move position center
for_window [app_id="Thunar"] floating enable, resize set 84% 84%, move position center
for_window [class="Thunar"] floating enable, resize set 84% 84%, move position center
for_window [app_id="gnome-disks"] floating enable, move position center
for_window [app_id="wdisplays"] floating enable, move position center
for_window [app_id="org.gnome.DiskUtility"] floating enable, move position center
for_window [app_id="mpv"] floating enable, resize set 78% 76%, move position center
for_window [app_id="imv"] floating enable, resize set 78% 76%, move position center
for_window [class="imv"] floating enable, resize set 78% 76%, move position center
for_window [app_id="zathura"] floating enable, resize set 76% 84%, move position center
for_window [app_id="org.pwmt.zathura"] floating enable, resize set 76% 84%, move position center
for_window [class="Zathura"] floating enable, resize set 76% 84%, move position center
for_window [class="Virt-manager"] floating enable, resize set 88% 86%, move position center
for_window [class="virt-manager"] floating enable, resize set 88% 86%, move position center
for_window [app_id="foot" title="sysmon"] floating enable, resize set 78% 78%, move position center
for_window [app_id="foot" title="fastfetch"] floating enable, resize set 96% 92%, move position center
for_window [app_id="scratchpad_term"] floating enable, resize set 82% 74%, move position center
for_window [class="pavucontrol"] floating enable, resize set 56% 60%, move position center
for_window [app_id="pavucontrol"] floating enable, resize set 56% 60%, move position center
for_window [app_id="foot --nmtui"] floating enable, resize set 700 520, move position center
for_window [app_id="brave-origin" title="^Picture-in-Picture$"] floating enable
for_window [app_id="foot --bluetoothctl"] floating enable, resize set 700 520, move position center

bar {
    position top
    swaybar_command waybar
}
